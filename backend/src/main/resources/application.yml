server:
  port: 8988

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lowcode_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  
  redis:
    host: localhost
    port: 6379
    password: jingneng2023.
    timeout: 6000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 2
        max-wait: -1ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      table-underline: true

jwt:
  secret: lowcode-secret-key-20240101-abcdefghijklmnopqrstuvwxyz
  expiration: 86400000

logging:
  level:
    com.lowcode: debug
    org.springframework.security: info